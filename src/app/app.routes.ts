import { Routes } from '@angular/router';
import { ApprovalLevel } from './feature-approval-levels/approval-level/approval-level';

export const routes: Routes = [
  { path:'approval-level', component: ApprovalLevel }
];
